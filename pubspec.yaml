name: heke_support
description: A new Flutter project.

publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^0.1.3
  firebase_core: ^1.11.0
  firebase_auth: ^3.3.5
  cloud_firestore: ^3.1.6
  fluttertoast: 8.0.6
  image_picker: 0.7.4
  shared_preferences: 2.0.5
  firebase_storage: 8.0.5
  intl: 0.17.0
  firebase_messaging: ^11.2.5
  #  flutter_local_notifications: 5.0.0+4
  photo_view: 0.12.0
  provider: ^6.0.1
  firebase_functions_dart: ^0.1.0-dev.2
  flutter_device_type: ^0.4.0
  modal_progress_hud_nsn: ^0.1.0-nullsafety-1


  google_fonts: ^2.1.0
  flutter_svg: ^0.22.0
  fl_chart: ^0.36.1
  data_table_2: ^2.0.3
  skeletons: ^0.0.3
  cached_network_image: ^3.2.0
  lottie: ^1.2.1



dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^1.0.0


# The following section is specific to Flutter.
flutter:
  uses-material-design: true

  assets:
    - assets/images/
    - assets/icons/
    - assets/sticker_images/
    - assets/gif/
